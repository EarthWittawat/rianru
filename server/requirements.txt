fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
neo4j==5.27.0
httpx==0.28.1
pytest==8.3.4
ruff==0.8.6
pdfplumber==0.11.10
nbformat==5.11.1
sentence-transformers==5.3.0

# torch arrives via sentence-transformers, but plain PyPI serves a CPU-only
# build. This machine has an RTX 5070 Ti (Blackwell, sm_120), which needs a
# CUDA 12.8 build — older cu121 wheels have no sm_120 kernels:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# sentence-transformers then picks up the GPU on its own.
# Stop any process importing torch before reinstalling; on Windows a running
# server holds torch/_C.pyd open and the uninstall half-fails.
langchain==1.4.0
langchain-openai==1.6.0

# Late-interaction retrieval (ColBERT). Pulls its own sentence-transformers and
# transformers pins, which is why the version above moved down rather than up.
pylate==1.6.0
