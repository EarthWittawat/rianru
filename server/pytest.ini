[pytest]
# Make `pytest` work the same as `python -m pytest`: without this the app
# package is only importable when the caller happens to put server/ on the path.
pythonpath = .
markers =
    slow: hits the live vLLM gateway; deselect with -m "not slow"
